# AI Agent System Configuration

# LLM Provider Configuration
LLM_PROVIDER=openai  # Options: openai, anthropic, mock
LLM_API_KEY=your_api_key_here
LLM_MODEL=gpt-4  # or claude-3-opus-20240229 for Anthropic

# USB Identity Storage
# Path to USB drive where agent identity is stored
USB_STORAGE_PATH=/mnt/usb_agent

# Nextcloud Working Memory
# Nextcloud WebDAV URL and credentials
NEXTCLOUD_URL=https://your-nextcloud.com/remote.php/dav/files/username
NEXTCLOUD_USERNAME=your_username
NEXTCLOUD_PASSWORD=your_password
NEXTCLOUD_BASE_PATH=agent_working_memory

# Database Configuration (Read-Only)
DATABASE_TYPE=sqlite  # Options: sqlite, postgresql, mysql
DATABASE_PATH=/path/to/database.db
# For PostgreSQL/MySQL:
# DATABASE_CONNECTION_STRING=postgresql://user:pass@localhost/dbname

# Agent Configuration
MAX_ITERATIONS=10
TEMPERATURE=0.7
LOG_LEVEL=INFO

# GitHub (for development only)
# GitHub is used only for code management and versioning
# It does not run the agent or host the model
GITHUB_REPO=your-username/agent-repo
